<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:app="clr-namespace:BestPractices"
             BindingContext="{Static app:ViewModelLocator.SearchViewModel}"
             x:Class="BestPractices.MainPage">
    <ContentPage.Resources>
     
    </ContentPage.Resources>
    <StackLayout Orientation="Vertical">
        <Label Margin="0, 20, 0,0" HorizontalOptions="Center" FontSize="Title">
            Search movie
        </Label>
        <BoxView Margin="0, 10, 0, 25" HeightRequest="1" Color="LightGray" />
        <Entry Margin="20" Text="{Binding SearchInput, Mode=TwoWay}"></Entry>
        <Button HorizontalOptions="Center" WidthRequest="150" Command="{Binding SearchCommand}" Text="Search"></Button>
        <Button HorizontalOptions="Center" WidthRequest="150" Command="{Binding ClearCommand}" Text="Clear"></Button>
        <ActivityIndicator IsVisible="{Binding Loading}" Margin="0,50" HorizontalOptions="Center" IsRunning="True"></ActivityIndicator>

        <StackLayout Orientation="Horizontal" VerticalOptions="EndAndExpand">
            <Button Command="{Binding GoToTrendingCommand}" Text="Trending"></Button>
        </StackLayout>
            
    </StackLayout>
</ContentPage>
